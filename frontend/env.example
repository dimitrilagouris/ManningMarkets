# MANNING MARKETS FRONTEND ENVIRONMENT FILE (.env)

# IMPORTANT: Replace 98.89.34.153 with your current EC2 Public IPv4 Address.

# --- API and WebSocket Base URLs (HTTPS/WSS Ready) ---
# Nginx handles routing, so no ports are needed on the public IP

# Use HTTPS for API calls
REACT_APP_DJANGO_API_BASE=https://98.89.34.153

# Use WSS for WebSocket connections
REACT_APP_WS_BASE_URL=wss://98.89.34.153